{
    "version": "2.0.0",
    "type": "shell",
    "options": {
        "cwd": "${workspaceRoot}"
    },
    "tasks": [
        {
            "label": "build",
            // "command": "CGO_ENABLED=1 go build -race -o bin ./cmd/...",
            "command": "go build -o bin ./cmd/...",
            "group": {
                "kind": "build",
                "isDefault": true
            },
        }
    ]
}